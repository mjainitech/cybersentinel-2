# Copy to .env and adjust if your backend runs somewhere other than
# the default. Never put third-party API keys here — those belong
# only in server/.env, since anything in a Vite .env is bundled into
# the client-side JavaScript and is NOT private.

VITE_API_BASE_URL=http://localhost:8787
